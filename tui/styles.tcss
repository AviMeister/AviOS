/* Styles the read-only Textual interface so AviOS stays clear and calm to use. */

#dashboard_body {
    padding: 1 2;
}

#dashboard_body Static {
    margin-bottom: 1;
}

DataTable {
    height: 1fr;
}
